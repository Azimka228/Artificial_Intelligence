import React from "react";
import styles from './ScrollToStart.module.scss'

const ScrollToStart = () => {
	return (
		<div className={styles.main}>
			<a href="#header">SCROLL</a>
		</div>
	);
};

export default ScrollToStart;